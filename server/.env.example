# --- Email (existing) ---
RESEND_API_KEY=
ADMIN_EMAIL=
RESEND_FROM=onboarding@resend.dev
PORT=3001

# --- Postgres catalog + admin auth ---
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/kha_mobile
JWT_SECRET=change-this-to-a-long-random-string

# Comma-separated browser origins allowed to call this API (CORS).
# Local Vite ports (8080, 5173, 4173) are always merged in server/index.js so
# localhost can call a deployed API; still list your live storefront here.
# FRONTEND_ORIGIN=http://localhost:8080,http://localhost:5173,https://khamobile.store
FRONTEND_ORIGIN=http://localhost:8080,http://localhost:5173,https://khamobile.store

# Canonical public storefront URL (no trailing slash). Same host should appear in FRONTEND_ORIGIN.
SITE_URL=https://khamobile.store

# Public base URL for uploaded files (must match the URL the browser uses to load /uploads/*).
# On Render, set to your service URL, e.g. https://kha-mobile.onrender.com (no trailing slash).
# Used in JSON for product images and when generating upload response URLs.
API_PUBLIC_URL=http://localhost:3001
